# fetch_engagement_data
Retrieves traffic and engagement metrics from the Google Analytics or CMS API.

## Parameters
- `time_period`: (string) e.g., "last_24_hours" or "last_7_days".

## Returns
- `posts`: (array) Objects containing `post_id`, `title`, `views`, and `engagement_rate`.